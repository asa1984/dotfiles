{
  inputs,
  pkgs,
  hostname,
  username,
  ...
}: {
  imports =
    [
      ./hardware-configuration.nix

      ../../modules/flatpak.nix
      ../../modules/fcitx5.nix
      ../../modules/fonts.nix
      ../../modules/gaming.nix
      ../../modules/i18n.nix
      ../../modules/networking.nix
      ../../modules/nix.nix
      ../../modules/sound.nix
      ../../modules/system-tools.nix
      ../../modules/virtualisation.nix
      ../../modules/xremap.nix
      ../../modules/xserver.nix
    ]
    ++ (with inputs.nixos-hardware.nixosModules; [
      common-cpu-amd
      common-gpu-amd
      common-pc-ssd
    ]);

  boot = {
    loader = {
      systemd-boot.enable = true;
      efi.canTouchEfiVariables = true;
    };
    kernelPackages = pkgs.linuxPackages_xanmod_latest;
  };

  networking.hostName = hostname;

  nixpkgs.config.allowUnfree = true;

  # Don't touch this
  system.stateVersion = "22.11";

  users.users."${username}" = {
    isNormalUser = true;
    shell = pkgs.zsh;
    extraGroups = [
      "networkmanager"
      "wheel"
      "audio"
      "video"
    ];
  };

  environment.systemPackages = with pkgs; [
    bottom
    direnv
    zellij
  ];

  programs = {
    zsh.enable = true;
    git.enable = true;
    neovim = {
      enable = true;
      defaultEditor = true;
      viAlias = true;
      vimAlias = true;
    };
    droidcam.enable = true;
  };

  # Use session generated by home-manager
  services.xserver = {
    displayManager = {
      # Set up dual monitors
      setupCommands = ''
        ${pkgs.xorg.xrandr}/bin/xrandr --output DP-1 --mode 2560x1440 --rate 165 --output DP-2 --auto --left-of DP-1
      '';

      lightdm = {
        enable = true;
        background = pkgs.fetchurl {
          url = "https://i.redd.it/49mj5c88ndla1.jpg";
          sha256 = "sha256-idMl5YkMMrXfBW36eG0buuJZ1IjmZLG/5TwfVROmC2s=";
        };
      };
      defaultSession = "home-manager";
    };
    desktopManager = {
      session = [
        {
          name = "home-manager";
          start = ''
            ${pkgs.runtimeShell} $HOME/.hm-xsession &
            waitPID=$!
          '';
        }
      ];
    };
  };
}
